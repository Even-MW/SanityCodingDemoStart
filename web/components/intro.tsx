import { CMS_NAME, CMS_URL } from "../lib/constants";

export default function Intro() {
    return (
        <section className="flex-col flex mt-16 mb-16 items-center md:flex-row md:mb-12 md:justify-between">
            <h1 className="font-bold leading-tight tracking-tighter text-6xl md:pr-8 md:text-8xl">The Blog.</h1>
        </section>
    );
}
